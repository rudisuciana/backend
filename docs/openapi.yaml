openapi: 3.0.3
info:
  title: PPOB Backend Blueprint API
  version: 1.0.0
  description: API blueprint for website and user channels with Bearer access token (website products) and x-api-key authorization (user routes).
servers:
  - url: http://localhost:3000
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    ApiKeyAuth:
      type: apiKey
      in: header
      name: x-api-key
  schemas:
    WebsiteProduct:
      type: object
      properties:
        id:
          type: integer
        code:
          type: string
        name:
          type: string
        category:
          type: string
        price:
          type: number
        adminFee:
          type: number
        isActive:
          type: boolean
    UserProfile:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        balance:
          type: number
        status:
          type: string
        avatar:
          type: string
          nullable: true
    AuthTokens:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
    AccessTokenPayload:
      type: object
      properties:
        accessToken:
          type: string
paths:
  /health:
    get:
      tags: [System]
      summary: Check service health (MySQL and Redis)
      responses:
        '200':
          description: All services up
        '503':
          description: One or more services down
  /api/website/ping:
    get:
      tags: [Website]
      summary: Basic connectivity test for website client (public)
      responses:
        '200':
          description: Website API reachable
  /api/website/products:
    get:
      tags: [Website]
      security:
        - BearerAuth: []
      summary: List active PPOB products (requires login bearer access token)
      responses:
        '200':
          description: Product list
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/WebsiteProduct'
        '401':
          description: Missing or invalid bearer access token
  /api/user/ping:
    get:
      tags: [User]
      security:
        - ApiKeyAuth: []
      summary: Basic connectivity test for user client (requires API key and whitelisted IP)
      responses:
        '200':
          description: User API reachable
        '403':
          description: Invalid API key or IP is not allowed
  /api/user/profile:
    get:
      tags: [User]
      security:
        - ApiKeyAuth: []
      summary: Get current user account info by x-api-key and whitelisted IP (for information and transaction preparation)
      responses:
        '200':
          description: User profile found
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/UserProfile'
        '404':
          description: User not found
        '403':
          description: Invalid API key or IP is not allowed
  /api/auth/register:
    post:
      tags: [Auth]
      summary: Register manual user with Gmail and send OTP to email
      responses:
        '201':
          description: Registration successful
        '400':
          description: Invalid payload
  /api/auth/verify-otp:
    post:
      tags: [Auth]
      summary: Verify OTP for manual registration
      responses:
        '200':
          description: OTP verified
        '400':
          description: Invalid OTP or payload
  /api/auth/login:
    post:
      tags: [Auth]
      summary: Login using email or username and password (issues access and refresh token; access token is cached in Redis)
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AccessTokenPayload'
        '401':
          description: Invalid credentials
  /api/auth/refresh-token:
    post:
      tags: [Auth]
      summary: Refresh only access token using valid refresh token (refresh token itself is not rotated)
      responses:
        '200':
          description: Token refreshed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AuthTokens'
  /api/auth/forgot-password:
    post:
      tags: [Auth]
      summary: Send forgot-password OTP to user email
      responses:
        '200':
          description: OTP process accepted
  /api/auth/reset-password:
    post:
      tags: [Auth]
      summary: Reset password using email OTP
      responses:
        '200':
          description: Password reset successful
  /api/auth/google/register:
    post:
      tags: [Auth]
      summary: Register using Google account (Gmail only)
      responses:
        '201':
          description: Google registration successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AuthTokens'
  /api/auth/google/login:
    post:
      tags: [Auth]
      summary: Login using Google account
      responses:
        '200':
          description: Google login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AuthTokens'
